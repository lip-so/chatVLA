# Lightweight cloud deployment requirements
# Core web framework and server
Flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.6
python-dotenv>=1.0.0
gunicorn>=21.2.0
eventlet>=0.33.3

# Authentication
PyJWT>=2.8.0
bcrypt>=4.1.2

# Firebase (optional, but lightweight)
firebase-admin>=6.4.0

# Essential data processing (lighter versions)
numpy>=1.24.0,<2.0
pandas>=2.0.0
Pillow>=10.0.0

# Hardware communication
pyserial>=3.5
psutil>=5.9.0

# Utilities
PyYAML>=6.0.1
requests>=2.31.0

# Note: Heavy ML dependencies removed for lightweight deployment
# Add these back if needed:
# torch, torchvision, transformers, datasets, opencv-python, 
# scipy, matplotlib, scikit-learn, clip-by-openai, huggingface-hub