# Skip GitHub Pages deployment
name: Skip Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  skip-pages:
    runs-on: ubuntu-latest
    steps:
      - name: Skip Pages Deployment
        run: |
          echo "GitHub Pages deployment is disabled for this repository"
          echo "This repository uses Railway for deployment instead"
          exit 0