# Minimal production requirements for ChatVLA Backend
# Core Flask dependencies
Flask==2.3.3
flask-cors==4.0.0
flask-socketio==5.3.6
python-dotenv==1.0.0

# Production server
gunicorn==21.2.0
eventlet==0.33.3
python-socketio==5.10.0

# Essential utilities
PyYAML==6.0.1
requests==2.31.0
Pillow==10.0.0

# Optional authentication (will gracefully fail if not available)
PyJWT==2.8.0
bcrypt==4.1.2

# Optional Firebase (will gracefully fail if not available)
firebase-admin==6.4.0

# Serial communication
pyserial==3.5
psutil==5.9.0

# Basic data processing
numpy==1.24.3
pandas==2.0.3

# Optional ML dependencies (commented out for minimal deployment)
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0
# datasets>=2.12.0
# opencv-python>=4.8.0
# scipy>=1.10.0
# matplotlib>=3.7.0
# scikit-learn>=1.3.0
# clip-by-openai>=1.0
# huggingface-hub>=0.16.0
